---
import FeedLink from "./FeedLink.astro"
import BlogLink from "./BlogLink.astro"

const { feed } = Astro.props
---

<a
  class="text-green no-underline flex-1 hover:text-green-h hover:underline"
  href={`/sites/${feed.slug}/`}>{feed.title}</a
>
<BlogLink url={feed.htmlUrl} />
<FeedLink url={feed.xmlUrl} />
