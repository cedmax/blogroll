<div class="prose bg-card border border-border rounded py-6 px-5">
  <slot />
</div>

<style>
  @reference "../styles/global.css";

  .prose :global(h1) {
    @apply text-xl font-bold mb-3 text-ink;
  }

  .prose :global(h2) {
    @apply text-lg font-bold mb-3 text-ink;
  }

  .prose :global(h3) {
    @apply text-base font-bold mb-3 text-ink;
  }

  .prose :global(p) {
    @apply text-sm text-ink-muted leading-relaxed mb-3;
  }

  .prose :global(ul) {
    @apply text-sm text-ink-muted leading-relaxed list-disc pl-5 mb-3 space-y-1;
  }

  .prose :global(a) {
    @apply underline;
  }

  .prose :global(code) {
    @apply text-ink break-all;
  }
</style>
